apiVersion: v1
kind: Service
metadata:
  name: cvss-frontend
spec:
  type: NodePort
  selector:
    app: cvss-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
